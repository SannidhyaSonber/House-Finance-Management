Create a folder named "Data Base" where the code.py file is saved.
Without the folder it won't run.
The software has password safety and data encription over Data Base
It offeres many features, options and custimizations for the user
It is multi user program.
and many tools embedded
